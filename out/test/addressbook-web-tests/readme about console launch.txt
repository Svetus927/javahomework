конфигурац файлы testng-...xml  сод инфу что запускать причем порядок тестов (тестовых групп) не определен, а порядок выполнения
 классов в них как указано так и будет
Из консоли запускать проект так gradlew test ( предвариетльно локализовавшись  в нужной папке и имея wrapper для gradle, НО
по умолчанию gradle ищет тесты с использованием фреймворка junit, поэтому обязатеьно н прописать в build.gradle строчки
test  {useTestNG() }
Если мы хотим сделать две отд задачи  например для групп и контактов и запускать их по отдельности то нужно прописать не test и т.д.
а
task testgroups(type: Test) {
  useTestng {
   suites '...xml'
  }
}
и  тогда из ком строки м выполнять отдельно задачу
gradlew testgroups

Если вып-ие из ком строки по каким то причинам не идет то вып команду
gradlew cleanTest test

******************
для того чтобы передать браузер кот н использовать при запуске нужно:
 ИЗ СРЕДЫ РАЗРАБОТКИ:
   для нужных тестов в параметрах конфигурации  там где
  vm options  дописать : -Dbrowser=firefox  // browser это имя сист переменной которая будет считываться в testbase при инициализации
 ( System.getProperty("browser")   считает это значение из конфигурации .  Если это св во в конфи ции не указать то если нет
 деф значения то все упадет :  System.getProperty("browser", BrowserType.Chrome)
 ИЗ КОМ СТРОКИ:
  подготовка - в параметрах задачи task в файле build.gradle дописать systemProperty 'browser', "${browser}" ///  В двойных кавычках
  это св во сборщика
  команда -    gradlew  -Pbrowser=firefox testgroups

*****************

про файлы  св-в  * properties (поддерживается стандартной  библиотекой java )
Например можно использовать его для выбора окружения где тестить:
Шаги:
1. В файле сборщика build.gradle для зачах нужно указать свойство сборщика, назовем его target чтобы при необх ти
из ком строки задавать нужное нам окружение
2. При  инициализации напрмер в конструкторе application manager задавать считываение этого параметра или значение по дефолту.






